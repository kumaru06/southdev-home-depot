
# Environment files (contain secrets)
.env
.env.production

# Deployment archives (too large for git)
southdev-hostinger.zip
*.zip

# Config (may contain fallback secrets)
config/config.php

# Ignore local conversation/log dumps
CONVERSATION_RAW.txt
CONVERSATION_LOG.md
CONVERSATION_SAVED_*.txt

# Local k6 load tests — keep on disk, do not commit or FTP-deploy via git status
tools/load-tests/
